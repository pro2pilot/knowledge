# Hermes .knowledge bridge

{{TRUST_ROUTING}}

Hermes currently uses the generic `AGENTS.md` bridge for `.knowledge`. This integration does not create a Hermes vendor folder because no stable public repo-local rules directory is bundled here.

{{FINAL_REPORT_CONTRACT}}

For concurrent agent work, set a stable `KNOWLEDGE_AGENT_ID` and use separate git worktrees/branches.
