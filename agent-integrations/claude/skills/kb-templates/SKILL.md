---
name: kb-templates
description: List or apply official .knowledge templates for a project type.
---

Use:

`node .knowledge/tools/apply-template.js --list`

Then apply a matching template, for example:

`node .knowledge/tools/apply-template.js nextjs-saas`

Templates are advisory. Verify all critical paths against current code and tests.
