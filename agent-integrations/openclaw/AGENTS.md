# OpenClaw .knowledge instructions

{{TRUST_ROUTING}}

OpenClaw can use repo-local `AGENTS.md` instructions and workspace skills. This integration installs the shared `.knowledge` skill set under `.agents/skills/`.

{{FINAL_REPORT_CONTRACT}}

For concurrent agent work, set a stable `KNOWLEDGE_AGENT_ID` and use separate git worktrees/branches.
