# .knowledge 3.2.0 10/10 Inventory

Generated by: codex-goal-10-of-10

## 1. Current branch

Free/core repo: `feat/knowledge-3-2-0-inspector-team-mode`

Workspace note: outer workspace root is not a git repository. The active free/core source checkout is `knowledge-3.2.0/`.

Paid Inspector directory: adjacent `pro2pilot-inspector/`; it is not a git repository in this workspace.

## 2. Current test status before fixes

Baseline commands run before new implementation work:

- `node tools/self-test-install-policy.js`: failed 10/11. Failure: `metrics/external_memory.json` was staged by `git add .` after fresh flow release.
- `node tools/self-test-memory-providers.js`: pass.
- `node tools/self-test-external-memory.js`: pass.
- `node tools/validate-paid-manifest.js --json`: pass.
- `node tools/self-test-team-mode.js`: inconclusive baseline because it ran concurrently with install-policy and saw `.self-test-tmp` nested repos. It must be rerun alone after cleanup.

## 3. Core files found

Source files are in `knowledge-3.2.0/`. A depth-limited file inventory was written to `.qa-tmp/knowledge-files.txt` because this Windows sandbox denied writes to `C:\tmp`.

Key core areas found:

- `tools/`
- `templates/git-policy/`
- `memory-providers/`
- `external_memory/`
- `docs/`
- `inspector/`
- `maintenance/`
- `models/`
- `agent-integrations/`

## 4. Paid Inspector directory found

`pro2pilot-inspector/` existed with:

- `README.md`
- `pro2pilot-inspector.zip`
- `memory-providers/graphiti/manifest.json`
- `memory-providers/zep/manifest.json`
- `versions/0.1.0/commercial-model.json`
- `versions/0.1.0/memory-governance.json`

It had no runnable app before this pass.

## 5. Claude MEM leftovers

Allowed legacy/migration references found in docs, migration inventory, doctor checks, memory provider detection, and tests.

No active Claude MEM provider manifest or install button should remain in free/core. Legacy detection remains advisory-only.

## 6. Mem0/Pinecone state

Mem0 OSS:

- manifest exists under `memory-providers/mem0/manifest.json`;
- recommended optional local/free provider;
- previous implementation was mostly manifest/status/receipt level;
- this pass adds `tools/memory-mem0.js` with dry-run/test-adapter commands and honest `runtime_not_installed` status.

Pinecone:

- manifest exists under `memory-providers/pinecone/manifest.json`;
- remains optional vector/cloud retrieval bridge;
- status mode is offline and redacts API-key-like values.

## 7. Graphiti/Zep paid state

Graphiti and Zep are paid Inspector provider contracts only. They live under adjacent paid workspace `pro2pilot-inspector/memory-providers/`.

## 8. Current UI implementation

Before this pass, free Visual Inspector generated a useful but mostly single-page static HTML. `serve-inspector.js` served a separate minimal page, not the generated UI.

This pass replaces generated UI output with a tabbed static shell and updates `serve-inspector.js` to serve generated `inspector/index.html`.

## 9. Packaging/release scripts

Found:

- `tools/package-release.js`
- `tools/self-test-install-policy.js`

Missing before this pass:

- `tools/validate-release-artifact.js`

Release blockers identified:

- installed `.knowledge.gitignore` missed `metrics/external_memory.json`;
- package release did not explicitly exclude Mem0/legacy provider runtime state, `metrics/external_memory.json`, or generated `inspector/index.html`;
- implementation docs contained local workspace paths and should not ship as install artifact.

## 10. Existing failures before fixes

- install-policy: 10/11 due `metrics/external_memory.json` not ignored.
- Team mode baseline must be rerun serially after `.self-test-tmp` cleanup.
- Paid Inspector was not a runnable app.
- Free Inspector was not tabbed enough for the requested acceptance criteria.
- Release artifact validator was missing.
