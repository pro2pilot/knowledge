# Routing Bundle Maintenance

Use when preparing a low-token first-read context for an agent.

Steps:

1. Run `node .knowledge/tools/build-routing-bundle.js`.
2. Read `.knowledge/maintenance/routing_bundle.json`.
3. Confirm it points to only relevant module cards and source files.
4. If high-risk modules are listed, require code recheck before behavior claims.
