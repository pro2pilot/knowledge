# Agent Report Footer

Connected agents can append a short `.knowledge` status block after work.

Modes:

- `off`
- `compact`
- `full`
- `only when trust incomplete`

Generate a footer:

```bash
node .knowledge/tools/agent-footer.js --json
```

Token metrics are estimates unless a runtime provides exact usage.
