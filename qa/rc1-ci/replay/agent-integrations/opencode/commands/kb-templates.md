---
description: List or apply official .knowledge templates
agent: build
---

List templates:

!`node .knowledge/tools/apply-template.js --list`

Apply a matching template only when it fits the repository type. Template output is advisory until verified against code/tests.
