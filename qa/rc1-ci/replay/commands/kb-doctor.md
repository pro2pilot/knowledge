# kb-doctor

Validate `.knowledge` structure, JSON files, module references, tracked files, event logs, routing bundle, and search index.

```bash
node .knowledge/tools/doctor.js
```

Read after running:

- `.knowledge/maintenance/quality_report.json`
